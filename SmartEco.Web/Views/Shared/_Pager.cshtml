@model ViewPager
@using SmartEco.Web.Extensions
@using SmartEco.Web.Models
@using SmartEco.Web.Models.Filters
@using System.Net

@if (Model.Pager.EndPage > 1)
{
    <ul class="pagination">
        @if (Model.Pager.PageNumber > 1)
        {
            <li>
                @Html.ActionLink(WebUtility.HtmlDecode("&laquo;&laquo;"), "Index", Model.BaseFilter.CloneSetValue(nameof(Pager.PageNumber), 1))
            </li>
            <li>
                @Html.ActionLink(WebUtility.HtmlDecode("&laquo;"), "Index", Model.BaseFilter.CloneSetValue(nameof(Pager.PageNumber), Model.Pager.PageNumber - 1))
            </li>
        }

        @for (var page = Model.Pager.StartPage; page <= Model.Pager.EndPage; page++)
        {
            <li class="@(page == Model.Pager.PageNumber ? "active" : "")">
                @Html.ActionLink(page.ToString(), "Index", Model.BaseFilter.CloneSetValue(nameof(Pager.PageNumber), page))
            </li>
        }

        @if (Model.Pager.PageNumber < Model.Pager.TotalPages)
        {
            <li>
                @Html.ActionLink(WebUtility.HtmlDecode("&raquo;"), "Index", Model.BaseFilter.CloneSetValue(nameof(Pager.PageNumber), Model.Pager.PageNumber + 1))
            </li>
            <li>
                @Html.ActionLink(WebUtility.HtmlDecode("&raquo;&raquo;"), "Index", Model.BaseFilter.CloneSetValue(nameof(Pager.PageNumber), Model.Pager.TotalPages))
            </li>
        }
    </ul>
}